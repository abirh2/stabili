# Optional token used only by local/build-time NYC Open Data ingestion scripts.
# Never expose this value through Vite or browser-side JavaScript.
NYC_OPEN_DATA_APP_TOKEN=

# Set to 1 to bypass and refresh data/intermediate/api-cache during ingestion.
NYC_OPEN_DATA_CACHE_REFRESH=0
