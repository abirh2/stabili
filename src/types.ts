/**
 * Public type entry point.
 *
 * The normalized generated-data contract is exported from `data/schema`.
 * The current hand-authored demo UI still uses the types in `types/legacy` until
 * a later change deliberately replaces the mock data.
 */
export * from './data/schema';
export * from './types/legacy';
